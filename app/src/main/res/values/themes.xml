<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DelegateAdapterWithExpandableItems" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DelegateAdapterWithExpandableItems" parent="Base.Theme.DelegateAdapterWithExpandableItems" />

    <style name="App.Style.MaterialCardView" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">6dp</item>
        <item name="cardElevation">4dp</item>
        <item name="cardUseCompatPadding">true</item>
    </style>

    <style name="roundedImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
</resources>